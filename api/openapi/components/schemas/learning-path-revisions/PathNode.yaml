type: object
description: One included guide in a path revision.
properties:
  guide_base_id:
    $ref: '../common/Uuid.yaml'
  guide_id:
    description: The pinned guide variant (always set); content is read live through it.
    $ref: '../common/Uuid.yaml'
  is_target:
    type: boolean
    example: true
  note:
    type: [string, 'null']
    example: Optional curator note
required: [guide_base_id, guide_id, is_target]
