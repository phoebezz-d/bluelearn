type: string
description: >
  An objective revision is either an editable draft or published. Objectives have
  no review gate, so there is no submitted/approved/rejected state as on guides.
enum: [draft, published]
example: draft
