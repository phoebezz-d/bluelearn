# Path items for /subjects. Referenced from ../openapi.yaml.
/subjects:
  get:
    tags: [subjects]
    summary: List all subjects
    operationId: listSubjects
    responses:
      '200':
        description: All subjects
        content:
          application/json:
            schema:
              type: object
              properties:
                subjects:
                  type: array
                  items:
                    $ref: '../components/schemas/subjects/Subject.yaml'
              required: [subjects]
      '501':
        $ref: '../components/responses/_shared.yaml#/NotImplemented'
  post:
    tags: [subjects]
    summary: Create a subject
    operationId: createSubject
    security:
      - bearerAuth: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/subjects/CreateSubjectRequest.yaml'
    responses:
      '201':
        description: The created subject
        content:
          application/json:
            schema:
              type: object
              properties:
                subject:
                  $ref: '../components/schemas/subjects/Subject.yaml'
              required: [subject]
      '501':
        $ref: '../components/responses/_shared.yaml#/NotImplemented'

/subjects/{slug}:
  parameters:
    - name: slug
      in: path
      required: true
      schema:
        type: string
      example: math
  get:
    tags: [subjects]
    summary: Get subject metadata
    description: Subject metadata only; the tagged topic list is a separate call.
    operationId: getSubject
    responses:
      '200':
        description: The subject
        content:
          application/json:
            schema:
              type: object
              properties:
                subject:
                  $ref: '../components/schemas/subjects/Subject.yaml'
              required: [subject]
      '501':
        $ref: '../components/responses/_shared.yaml#/NotImplemented'

/subjects/{slug}/guides:
  parameters:
    - name: slug
      in: path
      required: true
      schema:
        type: string
      example: math
  get:
    tags: [subjects]
    summary: List topics tagged with this subject
    description: Alphabetical list of topics carrying this subject tag.
    operationId: listSubjectGuides
    responses:
      '200':
        description: Tagged topics
        content:
          application/json:
            schema:
              type: object
              properties:
                guides:
                  type: array
                  items:
                    $ref: '../components/schemas/subjects/SubjectGuideListItem.yaml'
              required: [guides]
      '501':
        $ref: '../components/responses/_shared.yaml#/NotImplemented'
